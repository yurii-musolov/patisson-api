# Examples of how to use bybit-sdk

This directory contains a number of examples showcasing various capabilities of the `bybit-sdk` crate.

All examples can be executed with:

```sh
cargo run --example $name
```
